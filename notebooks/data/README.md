# Data from the marine tracking device

The current folder contains two files.

`dive_data_simu.xlsx` is a sample file to explain the data structure. It contains a header sheet with metadata and a samples sheet with temperature and pressure readings.

`dive_data_simu.bin` is a binary file that simulates the raw data received from the device.