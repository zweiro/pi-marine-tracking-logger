# MongoDB 7.0 configuration file
# Note: journaling is always enabled by default in MongoDB 7.0+

storage:
  dbPath: /data/db

systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

net:
  port: 27017
  bindIp: 0.0.0.0

security:
  authorization: disabled
